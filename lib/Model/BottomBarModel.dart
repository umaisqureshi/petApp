import 'package:flutter/material.dart';

class BottomBarModel {
  late Icon icon;
  late bool isSelected;
  late int id;

  BottomBarModel({required this.icon, required this.isSelected , required this.id});
}
